# Session — 2026-06-21 · Phase 0

## What we did
- **Fixed Ask KRNL** (`retrieval.py`): `qdrant.search()` → `query_points().points`;
  `text_search` now passes `options={"config":"english"}`. Verified end-to-end.
- **Sync Now button** (`InboxScreen.tsx`): replaced the dead hamburger; spinner, polls
  `/sync/status`, refreshes list.
- **Dev sync fallback capped** (`sync_task.py` `max_emails`; `sync.py` passes 3) so no-Redis
  runs return fast.
- **Removed Ask KRNL mock Q&A** (`AskKrnlScreen.tsx`).
- **Collapsible "Full Message"** (`EmailDetailScreen.tsx`).
- **Service worker made PROD-only** (`main.tsx`) — its stale-while-revalidate cache was
  serving old code in dev.

## Issues found (carried forward)
- Re-sync created duplicate event rows → motivated Phase 1.
- "Full Message" collapsible appeared on only 1 mail though all rows have `full_body` —
  suspect `EmailDetailScreen` falling back to `previewData`; still open.

## How to proceed
- Commit Phase 0 on a branch, then Phase 1 (dedup). (Done next session.)
